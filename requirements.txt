# Core FastAPI and web framework
fastapi>=0.100.0
uvicorn>=0.20.0
python-dotenv>=1.0.0

# OpenAI and AI libraries
openai>=1.0.0
tiktoken>=0.4.0

# PDF processing
PyPDF2>=3.0.0
pdfplumber>=0.9.0

# Data processing and ML
numpy>=1.21.0,<2.0.0  # Pin numpy to 1.x for Python 3.8 compatibility
pandas>=1.5.0
scikit-learn>=1.2.0
transformers>=4.21.0

# Advanced PII Detection (Python 3.8 compatible versions)
# Microsoft Presidio for enterprise-grade PII detection
presidio-analyzer>=2.2.0,<2.3.0
presidio-anonymizer>=2.2.0,<2.3.0

# spaCy for NLP (Python 3.8 compatible)
spacy>=3.4.0,<3.7.0

# Additional ML models for better PII detection
# Note: Install spaCy model with: python -m spacy download en_core_web_sm

# HTTP and networking
requests>=2.28.0
httpx>=0.24.0

# Logging and utilities
loguru>=0.7.0

# Testing
pytest>=7.0.0
pytest-asyncio>=0.21.0

# AWS (optional)
boto3>=1.26.0

# Vector database (optional)
weaviate-client>=3.15.0

# MCP (optional) - Model Context Protocol
# Note: MCP package has build issues, using websockets only for now
# mcp functionality implemented directly in code
websockets>=12.0
